import { observer } from "mobx-react";
import MatchParticipantList from "./MatchParticipantList.react";

/**
 * React component for the match participant list.
 */
export default observer(MatchParticipantList);
